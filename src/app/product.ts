import { DecimalPipe } from "@angular/common";

export class Product {
    name!: string;
    image!: string;
    unitPrice!: number;
    description!: string;
    size!: number;
    concentration!: number;
}
